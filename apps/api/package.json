{
  "name": "@saas-pricing/api",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript business API scaffold for the SaaS pricing platform.",
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fastify": "^5.6.1",
    "jose": "^6.1.0",
    "zod": "^4.1.12"
  }
}
