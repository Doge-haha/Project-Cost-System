# 工作日志 - 2026-04-17

时间：2026-04-17 22:10:24 CST

## 本次关键决策

- 冻结旧 `Java/Spring Boot` 原型，归档到 `legacy/backend-java`。
- 正式采用 `TypeScript` 主业务后端 + `Python` AI Runtime + `TypeScript` MCP Gateway/Worker 的 AI-first 技术路线。
- 当前 `apps/api` 仍以内存仓储为主，允许继续推进业务主链；真实数据库事务能力在持久化阶段补齐。

## 架构与仓库状态

- 仓库主线已切换到：
  - `apps/api`
  - `apps/ai-runtime`
  - `apps/mcp-gateway`
  - `apps/worker`
  - `apps/frontend`
- 旧 Java 原型保留为参考实现，不再继续扩展。
- 项目已初始化 Git 仓库并完成首个提交：
  - commit: `a5271c2`
- 已推送到 GitHub：
  - [Project-Cost-System](https://github.com/Doge-haha/Project-Cost-System)

## 已完成工作

### 1. 基础底座

- JWT 鉴权接入。
- 统一异常处理接入。
- Zod 请求校验接入。
- 分页基线接入。
- `createApp` fallback 仓储改为共享实例，修复默认路径下数据不互通问题。
- `/health` 响应收敛为 `{ ok: true }`。
- JWT secret 最小长度校验已补。

### 2. 项目基础模块

- `project`
- `project_stage`
- `project_discipline`
- `project_member`
- 集中式项目授权服务

已支持：
- 项目列表/详情
- 阶段列表
- 专业列表
- 成员列表
- 项目默认 `price_version / fee_template` 绑定

### 3. 清单与版本链

- `bill_version`
- `bill_item`
- `bill_work_item`

已支持：
- 版本列表/创建
- 清单项列表/创建/更新
- 工作内容列表/创建
- `copy-from` 复制版本、清单项树和工作内容
- 提交前校验摘要
- `submit / withdraw`
- `submitted` 状态禁止继续写入
- 手工调价

### 4. 定额 / 价目 / 取费 / 计价

- `quota_line`
- `price_version / price_item`
- `fee_template / fee_rule`
- 计价引擎 `calculate`

已支持：
- 定额明细列表/创建/更新
- 价目版本列表
- 价目明细列表
- 取费模板列表/详情
- 单项计价
- 项目默认价目/取费模板驱动计价
- 计价结果回写 `bill_item`
- 版本级批量重算
- 项目级/上下文级批量重算

### 5. 汇总与偏差分析

已支持：
- `GET /v1/reports/summary`
- `GET /v1/reports/summary/details`

当前输出：
- 总项数
- 系统总额
- 最终总额
- 偏差金额
- 偏差率
- 偏差明细项（按绝对偏差排序）

### 6. 审核入口骨架

新增：
- `review_submission`

已支持：
- 提交审核
- 审核列表
- 审核通过
- 审核驳回

状态联动：
- 审核通过：`bill_version -> locked`
- 审核驳回：`bill_version -> editable`

## 本次修复的 review 问题

- 修复 `createApp` fallback 仓储实例不一致。
- 修复 `BillWorkItemService.createWorkItem` 通过更新接口做权限检查的 CQS 问题。
- 修复 `/health` 过度暴露。
- 修复 JWT secret 无长度约束。

## 当前仍保留的工程债

- `transaction runner` 仍为占位实现，进入真实数据库持久化前必须补齐。
- `create-app.ts` 路由聚合仍偏大，后续应按模块拆分。
- `worker / mcp-gateway` 目前仍是轻骨架，尚未承载真实业务任务。
- 变更单、签证单、报表任务、AI runtime 业务能力仍未进入主实现阶段。

## 验证结果

已通过：

- `npm --workspace @saas-pricing/api test`
- `npm --workspace @saas-pricing/api run typecheck`
- `npm test`
- `npm run typecheck`

## 当前可用接口范围

- `/health`
- `/v1/me`
- `/v1/projects`
- `/v1/projects/:projectId`
- `/v1/projects/:projectId/stages`
- `/v1/projects/:projectId/disciplines`
- `/v1/projects/:projectId/members`
- `/v1/projects/:projectId/default-pricing-config`
- `/v1/projects/:projectId/bill-versions`
- `/v1/projects/:projectId/bill-versions/:billVersionId/recalculate`
- `/v1/projects/:projectId/recalculate`
- `/v1/projects/:projectId/bill-versions/:billVersionId/copy-from`
- `/v1/projects/:projectId/bill-versions/:billVersionId/validation-summary`
- `/v1/projects/:projectId/bill-versions/:billVersionId/submit`
- `/v1/projects/:projectId/bill-versions/:billVersionId/withdraw`
- `/v1/projects/:projectId/bill-versions/:billVersionId/items`
- `/v1/projects/:projectId/bill-versions/:billVersionId/items/:itemId/manual-pricing`
- `/v1/projects/:projectId/bill-versions/:billVersionId/items/:itemId/work-items`
- `/v1/projects/:projectId/bill-versions/:billVersionId/items/:itemId/quota-lines`
- `/v1/projects/:projectId/quota-lines/:quotaLineId`
- `/v1/price-versions`
- `/v1/price-versions/:priceVersionId/items`
- `/v1/fee-templates`
- `/v1/fee-templates/:feeTemplateId`
- `/v1/engine/calculate`
- `/v1/reports/summary`
- `/v1/reports/summary/details`
- `/v1/projects/:projectId/bill-versions/:billVersionId/reviews`
- `/v1/projects/:projectId/reviews`
- `/v1/projects/:projectId/reviews/:reviewSubmissionId/approve`
- `/v1/projects/:projectId/reviews/:reviewSubmissionId/reject`

## 下次启动的建议起点

建议按以下顺序继续：

1. 审计日志骨架
2. 审核列表筛选和版本摘要增强
3. 过程单据入口：
   - 变更单
   - 现场签证
   - 进度款
4. `worker` 承接异步报表或批量重算任务
5. 进入真实数据库持久化和事务实现
