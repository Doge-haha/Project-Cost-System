# 2026-04-20 开发工作总结

## 今日主线结论

今天把这个工作区从“后端数据库主路径已成形”继续推进到了“前端最小工作台已经能做项目/版本/清单/汇总/版本对比”的状态。

## 今日完成的后端与工程收口

- 收口了 `apps/api` 的 `database mode`
  - 增加严格装配校验
  - `database mode` 不再允许静默回退到 `InMemory*Repository`
  - 补了对应测试
- 收口了本地开发链路
  - 根 `package.json` 增加短命令
  - 根 `Makefile` 改成当前项目可用目标
  - README 改成默认推荐 `database mode`
  - 补了 `test:live-db`
- 跑通了真实 Postgres live smoke
  - `docker compose -f deploy/docker/docker-compose.dev.yml up -d --wait`
  - `DATABASE_URL=postgres://postgres:postgres@localhost:5432/saas_pricing npm --workspace @saas-pricing/api run test:live-db`
  - 真实容器里的 Postgres 验证通过
- 做了一轮 `mcp-gateway` / `worker` 结构整理
  - `mcp-gateway` 把 capabilities / permissions / responders 拆出
  - `worker` 把 CLI runtime 装配拆出

## 今日完成的前端工作

`apps/frontend` 已从占位目录升级成可运行的 `React + TypeScript + Vite` 工作台。

已完成：

- 前端骨架、Vite、Vitest、TypeScript 配置
- 统一 API client
- 开发连接面板
  - 支持 `VITE_API_BASE_URL` / `VITE_API_BEARER_TOKEN`
  - 也支持在侧边栏保存到 localStorage
- 页面主链
  - `/projects`
  - `/projects/:projectId`
  - `/projects/:projectId/bill-versions/:versionId/items`
  - `/projects/:projectId/summary`
- 项目首页
  - 摘要卡片
  - 重点项目区
  - 项目列表
- 项目详情页
  - 版本工作台卡片
  - 当前版本 dashboard 摘要
  - 进入清单页 / 汇总页
- 清单页
  - 层级表格
  - 版本切换
  - 父节点展开/折叠
- 汇总页
  - 版本上下文
  - 偏差重点项
  - `version-compare` 最小接入
  - 基准版本 / 对比版本选择
  - 差异重点项列表
- 共享组件
  - `BillVersionSelector`
  - `AppBreadcrumbs`

## 今日关键文件

### 后端 / 工程

- `apps/api/src/app/create-app-dependencies.ts`
- `apps/api/test/app-dependencies.test.ts`
- `apps/api/test/database-live-smoke.test.ts`
- `Makefile`
- `README.md`

### 前端

- `apps/frontend/src/lib/api.ts`
- `apps/frontend/src/lib/config.ts`
- `apps/frontend/src/features/shared/bill-version-selector.tsx`
- `apps/frontend/src/features/shared/breadcrumbs.tsx`
- `apps/frontend/src/features/projects/projects-page.tsx`
- `apps/frontend/src/features/projects/project-detail-page.tsx`
- `apps/frontend/src/features/projects/projects-dashboard.ts`
- `apps/frontend/src/features/projects/project-dashboard-summary.ts`
- `apps/frontend/src/features/projects/project-version-cards.ts`
- `apps/frontend/src/features/bills/bill-items-page.tsx`
- `apps/frontend/src/features/bills/bill-items-table.tsx`
- `apps/frontend/src/features/reports/summary-page.tsx`
- `apps/frontend/src/features/reports/summary-highlights.ts`
- `apps/frontend/src/features/reports/summary-compare.ts`

## 当前测试状态

今天反复验证并最终通过：

- `npm --workspace @saas-pricing/api run test:live-db`
- `npm --workspace saas-pricing-frontend test`
- `npm --workspace saas-pricing-frontend run typecheck`
- `npm run typecheck`
- `npm test`

其中：

- 整仓测试通过
- API live Postgres smoke 通过
- frontend 测试当前全绿

## 当前前端使用方式

先启动依赖：

```bash
cd "/Users/huahaha/WorkSpace/something/新点SaaS计价"
npm run dev:deps:up
```

API live smoke：

```bash
npm run api:test:live-db
```

前端启动：

```bash
VITE_API_BASE_URL=http://localhost:3000 \
VITE_API_BEARER_TOKEN="<your bearer token>" \
npm --workspace saas-pricing-frontend run dev
```

也可以先启动前端后，在左侧 sidebar 的开发连接面板里填 API 地址和 Bearer Token。

## 下次新对话建议的启动上下文

建议直接告诉新对话里的代理：

- 这个仓库已经完成今天的前端最小工作台搭建
- `database mode` 和 `live-db` 都已经跑通
- frontend 已有：
  - 项目首页 dashboard
  - 项目详情页版本工作台
  - 清单页层级表格 + 折叠
  - 汇总页偏差重点项
  - `version-compare` 最小接入
- 所有测试当前全绿
- 下一步优先项建议是：
  1. 把页面里的 `projectId` 替换成真实项目名上下文
  2. 给 `version-compare` 补完整差异表格
  3. 或继续增强清单页父级摘要 / 聚合信息

## 我建议的下一步

下次优先做：

- `projectId` 文本上下文清理
- `version-compare` 完整差异表格

因为现在功能链已经够了，最缺的是“看起来更像真实业务页面而不是联调页”。
