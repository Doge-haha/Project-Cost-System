# 2026-04-21 开发工作日志

## 今日主线结论

今天把“项目工作台聚合”这条线从统一入口继续推进到了“一期可操作闭环 + 导入任务真实化”的状态。

现在已经不是单纯把项目信息聚在一起，而是基本形成了：

- 工作台统一入口
- 摘要区看到待办 / 风险 / 导入状态
- 进入审核 / 过程单据 / 任务状态页继续处理
- 处理完成后带焦点返回、回流提示、最近动态联动
- 导入状态从异步任务占位升级成正式 `import_task`
- 任务状态页支持真实文件上传创建导入批次
- 导入批次详情支持解析概览、条目预览、失败原因归类、失败条目筛选、重试历史
- 失败批次支持下载错误报告，且已显式区分“导出当前筛选”和“导出整批”，文件名也会带上范围标识；当前筛选为空时会直接跳过空导出；导出区块已按范围分组收口，成功提示与复制链接提示都可直接回到对应失败原因视角，导出 loading 也已拆成分按钮状态
- `failureReason` 协作视角可在工作台 / 待办页 / 任务状态页之间保留、复制和清理

## 今日完成的主功能

### 1. 项目工作台聚合完成首版闭环

已完成：

- 新增项目级 `workspace` 聚合接口
- 聚合项目、阶段、专业、可见版本、当前用户权限摘要
- 前端项目详情页切换为优先消费 workspace 聚合结果
- 摘要区补齐：
  - `todoSummary`
  - `riskSummary`
  - `importStatus`

### 2. 待办处理链路已打通

已完成：

- 项目待办页 `projects/:projectId/inbox`
- 审核处理页 `projects/:projectId/reviews`
- 过程单据处理页 `projects/:projectId/process-documents`
- 任务状态页 `projects/:projectId/jobs`

这三条链已经具备：

- 从工作台摘要进入
- 在当前项目上下文中执行处理动作
- 返回时带焦点
- 处理完成后带刷新提示回流工作台 / 待办页

### 3. 工作台最近动态时间线已接通

已完成：

- 项目详情页右侧增加最近动态
- 动态来源首版复用项目审计日志
- 可点击回到：
  - 审核处理页
  - 过程单据处理页
  - 任务状态页
- 跳转后自动选中对应记录

### 4. 导入状态已升级为正式导入任务模型

已完成：

- 新增正式 `import_task` 模型
- 工作台 `importStatus` 改为基于导入任务聚合
- 知识提取 / 导入链路先创建导入任务，再创建异步任务
- 导入任务稳定返回：
  - `sourceFileName`
  - `sourceBatchNo`
  - `failureDetails`
  - `retryCount`
  - `retryLimit`
  - `canRetry`

### 5. 真实文件上传批次已接入

已完成：

- 新增上传入口：`POST /v1/projects/:projectId/import-tasks/upload`
- 首版支持：
  - `JSON` 数组
  - `JSONL`
  - `NDJSON`
- 任务状态页支持选择文件并上传
- 上传成功后自动生成导入批次并聚焦到新批次

### 6. 导入批次详情已补到可下钻状态

已完成：

- 导入任务页改成“列表 + 批次详情”
- 批次详情展示：
  - 来源
  - 格式
  - 文件名
  - 批次号
  - 导入统计
  - 失败摘要
  - 关联任务
  - 原始批次元数据
- 本次又继续补了：
  - 解析概览
  - 条目预览
  - 失败原因归类
  - 失败条目列表
  - 按失败原因筛选
  - 错误报告下载
  - 重试历史

### 7. 失败条目协作视角已接通

已完成：

- 任务状态页支持通过 `failureReason` 锁定当前失败原因视角
- 失败原因筛选会同步影响失败条目列表和重试提示文案
- 当前筛选链接可直接复制给协作同事
- 无效 `failureReason` 会自动从 URL 中清理，避免脏链接残留
- 工作台详情页、待办页、任务状态页之间会继续携带同一协作视角
- 在协作视角下返回工作台 / 待办页时，仍能保持导入问题的上下文

## 今日关键接口和页面

### 后端

- `GET /v1/projects/:projectId/workspace`
- `GET /v1/projects/:projectId/import-tasks`
- `GET /v1/projects/:projectId/import-tasks/:taskId/error-report`
- `POST /v1/projects/:projectId/import-tasks`
- `POST /v1/projects/:projectId/import-tasks/upload`
- `POST /v1/jobs/:jobId/retry`

### 前端页面

- `projects/:projectId`
- `projects/:projectId/inbox`
- `projects/:projectId/reviews`
- `projects/:projectId/process-documents`
- `projects/:projectId/jobs`

## 今日关键文件

### 后端

- `apps/api/src/modules/project/project-service.ts`
- `apps/api/src/app/register-project-routes.ts`
- `apps/api/src/app/register-ops-routes.ts`
- `apps/api/src/modules/import/import-task-repository.ts`
- `apps/api/src/modules/import/import-task-service.ts`
- `apps/api/src/modules/jobs/background-job-service.ts`
- `apps/api/drizzle/0009_secret_alice.sql`

### 前端

- `apps/frontend/src/features/projects/project-detail-page.tsx`
- `apps/frontend/src/features/projects/project-workspace-inbox-page.tsx`
- `apps/frontend/src/features/projects/project-reviews-page.tsx`
- `apps/frontend/src/features/projects/project-process-documents-page.tsx`
- `apps/frontend/src/features/projects/project-job-status-page.tsx`
- `apps/frontend/src/features/projects/failure-reason-label.ts`
- `apps/frontend/src/app/router.tsx`
- `apps/frontend/src/lib/api.ts`
- `apps/frontend/src/lib/types.ts`
- `apps/frontend/src/styles/global.css`

### 规范

- `openspec/changes/doc-normalize-v1-to-openspec/specs/project-workspace/spec.md`

## 今日验证结果

本轮反复通过过的命令包括：

- `npm --workspace @saas-pricing/api run test -- app.test.ts`
- `npm --workspace @saas-pricing/api run typecheck`
- `npm --workspace @saas-pricing/api run db:generate`
- `npm --workspace saas-pricing-frontend run test -- project-detail-page.test.tsx project-workspace-inbox-page.test.tsx project-reviews-page.test.tsx project-reviews-focus.test.tsx project-process-documents-page.test.tsx project-process-documents-focus.test.tsx project-job-status-page.test.tsx project-job-status-focus.test.tsx project-dashboard-summary.test.ts`
- `npm --workspace saas-pricing-frontend run typecheck`

今天最后一次确认通过的是：

- `npm --workspace @saas-pricing/api run test -- app.test.ts`
- `npm --workspace @saas-pricing/api run typecheck`
- `npm --workspace saas-pricing-frontend run test -- project-job-status-page.test.tsx project-job-status-focus.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

其中 `project-job-status-page.test.tsx` 已覆盖：

- 失败原因归类展示
- 失败条目按 `failureReason` 筛选
- 协作视角复制 / 清理
- 协作视角下的重试提示和回流链接
- 上传文件后新批次详情刷新

## 当前完成度判断

如果按“项目工作台一期”来判断，我会给到 `88%` 左右。

已经成型的部分：

- 统一入口
- 摘要聚合
- 待办处理
- 最近动态
- 回流提示
- 导入任务正式化
- 真实上传批次首版
- 失败条目定位
- 协作视角保留与回流

还没做满的部分主要是更细粒度的运营体验和问题定位体验。

## 下一步最建议继续做什么

我建议下一步优先做：

1. 继续补“失败条目运营体验”，比如更清晰的分组、批量处理或单条回看能力
2. 把工作日志 / spec / 接力说明同步到当前真实代码状态
3. 再决定是否切回主业务链或继续打磨项目工作台二期

## 当日晚些时候补充

主业务链回看后，先把 `reviews` 审核处理页补了一步更贴近真实协作的闭环：

- 本页点击 `通过 / 驳回 / 撤回` 时，会把当前处理对象同步写入 `reviewId` 和 `action`
- 取消处理或处理完成后，会自动清理这两个查询参数，避免把过期动作继续留在链接里
- 如果 URL 带着不存在的 `reviewId`，或动作已不再可执行，也会自动清理脏参数

这一步的意义不是再加展示，而是把“最近动态跳入审核页”这条链和“本页继续处理/复制当前链接”统一成同一套深链语义，避免审核页只能接住外部深链，却不能稳定生成自己的当前处理视角。

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-reviews-page.test.tsx project-reviews-focus.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

随后把同样的深链闭环补到了 `process-documents` 过程单据处理页：

- 本页点击 `提交 / 通过 / 驳回` 时，会同步写入 `documentId` 和 `action`
- 取消处理或处理完成后，会自动清理对应查询参数
- 如果链接中的单据不存在，或动作已经不可执行，也会自动清理脏参数

这样最近动态、待办页和过程单据处理页之间，不只是能跳转到单据页，还能稳定保留“当前正在处理哪一条、以什么动作处理”的上下文。

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-process-documents-page.test.tsx project-process-documents-focus.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

随后又回到项目工作台，把“最近动态”从可跳转再推进到了可交接：

- 最近动态里的审核、过程单据、异步任务条目，除了可点击进入处理页，现在还支持直接复制处理链接
- 复制出的链接会保留该条动态对应的处理上下文，例如审核 `reviewId/action`、过程单据 `documentId/action`、任务 `jobId` 与当前失败原因视角
- 这样工作台里的最近动态已经不只是“看发生了什么”，也能直接把对应处理入口发给协作同事

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-detail-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

最后把同样的协作动作补到了 `inbox` 待办页：

- 审核待办卡片现在支持直接复制处理链接，默认给出当前最可执行的审核动作入口
- 过程单据卡片现在也支持直接复制处理链接，默认给出当前最可执行的提交或审核入口
- 这样 `project-detail`、`inbox`、具体处理页这三层入口已经有一致的“可跳转 + 可复制处理链接”协作体验

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-workspace-inbox-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

最后又回到 `jobs` 任务状态页，把失败排查入口补成和其它层一致：

- 当前筛选运营摘要里现在支持直接复制“当前处理链接”，适合把当前失败原因子集直接发给协作同事
- 单条失败记录回看里也支持复制“当前处理链接”，会显式保留 `failedLine` 定位
- 这样 `project-detail`、`inbox`、`jobs` 三层入口都已经支持围绕当前处理上下文直接复制链接

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-job-status-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

我又把“复制链接成功反馈”从纯提示往前推了一步：

- `project-detail` 最近动态里，复制处理链接后现在会出现“打开刚复制入口”
- `inbox` 待办页里，复制审核/过程单据处理链接后也会出现同样的快速入口
- 这样复制动作不再只是“告诉你成功了”，而是能立刻回点校验，降低协作交接时的来回确认成本

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-detail-page.test.tsx project-workspace-inbox-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

随后把同样的“打开刚复制入口”反馈也补到了 `jobs` 任务状态页：

- 复制当前筛选链接后，现在可以立即点“打开刚复制入口”回到同一筛选视角
- 复制当前处理链接后，也可以立刻点回当前子集或单条失败记录定位
- 这样 `project-detail`、`inbox`、`jobs` 三层入口的复制反馈已经完全统一

补这个过程中，我顺手把 `project-job-status-page.test.tsx` 里那条超长主回归用例的超时上限提高到了 30 秒，避免它继续被测试时限而不是功能问题打断。

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-job-status-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`

原因：

- 失败条目明细、失败原因筛选和协作视角其实已经落地
- 当前更大的风险是“日志还停在旧阶段”，容易导致下次接手重复开发
- 在进入下一阶段前，先把文档口径和真实状态对齐，判断会更稳

如果继续顺着做，建议目标是：

- 把失败条目从“可筛选”继续推进到“可批量运营 / 可分享 / 可定位”
- 再评估是否要做单条重试、重新导入或更细的错误归类
- 若工作台一期先收口，则应把后续重点转回真实业务主链

## 本次补推进记录

基于上面的“下一步最建议继续做什么”，本次已先补一格最小闭环：

- 任务状态页里的失败条目支持继续定位到单条记录
- 当前 URL 会保留 `failedLine`，复制链接时可直接把协作同事带到同一条失败记录
- 若 `failedLine` 无效或和当前筛选不匹配，前端会自动清理该参数，避免脏深链继续传播
- 单条回看视角里已补上“上一条 / 下一条 / 返回失败列表”，方便围绕同一失败原因逐条排查
- 单条回看视角里已补上原始条目预览和字段交叉检查，能更快判断这条失败到底是缺字段、动作缺失还是值异常
- 当前失败原因视角里已补上轻量运营摘要，能先看到资源类型、动作分布、缺字段数量和可回看预览数量，再决定是否逐条处理
- 当前运营摘要里的资源类型和动作分布已支持直接点击收束到子集，并通过 URL 保留该子集视角
- 当资源类型 / 动作二级收束生效时，当前导出也会切到“当前子集导出”，避免导出范围和页面视角不一致
- 当前范围旁边已补上只读的批量动作建议区，会把“缺字段 / 映射集中 / 是否有原始预览 / 是否已收束到子集”直接转成处理建议
- 当前范围已支持一键复制“处理摘要”文案，可直接把当前子集、条目数量、预览覆盖和建议动作发给协作同事或上游排查
- 当前“处理摘要”已拆成“协作同事版”和“上游数据方版”两种复制出口，同一份统计会按接收对象切换文案重点
- 当前两类“处理摘要”都会自动附带当前页面链接，接收方可以直接打开同一失败原因 / 子集视角继续处理
- 当前“上游数据方版”处理摘要会额外附带建议导出文件名提示，方便对方把摘要、链接和导出材料直接对应起来

这样做的目的不是继续扩工作台壳层，而是把“问题定位体验”从“原因级”推进到“条目级”，仍然保持在当前导入运营主线上。

## 新账号接力时建议直接带上的上下文

建议下一次新对话直接说明：

- 当前仓库已经完成“项目工作台聚合”一期主链
- `workspace`、`inbox`、`reviews`、`process-documents`、`jobs` 都已落地
- `importStatus` 已经不是异步任务占位，而是正式 `import_task`
- 任务状态页已支持真实上传文件生成导入批次
- 导入批次详情已支持：
  - 解析概览
  - 条目预览
  - 失败原因归类
  - 失败条目筛选
  - 重试历史
- `failureReason` 协作视角已打通工作台 / 待办页 / 任务状态页
- 下一步优先看“文档对齐后还剩下哪些真实缺口”
- 继续改动时不要忘记同步：
- `openspec/changes/doc-normalize-v1-to-openspec/specs/project-workspace/spec.md`

## 备注

这一轮仓库里有不少并行中的改动，不要做大范围回滚。继续开发时优先沿当前工作台和导入任务主线往下补，尽量避免动到无关模块。

## 本次补推进记录（最近复制入口）

这一步继续把复制反馈从“瞬时提示”推进成“跨页可回看”：

- 新增了 `recent-processing-link.ts`，用同一个轻量 session 记忆收住最近一次复制的处理入口
- `project-detail`、`project-workspace-inbox`、`project-job-status` 现在都会展示“最近复制入口”
- 用户在这三页之间来回切换时，仍然可以直接打开最近一次复制的处理入口
- 页面上也提供了“清除最近复制记录”，避免旧的协作入口一直挂着干扰判断

这样现在的协作链路已经从：

- 复制成功后可立即打开

继续推进成：

- 切页后仍能回到最近一次交接的处理入口

本次补充验证通过：

- `npm --workspace saas-pricing-frontend run test -- project-detail-page.test.tsx project-workspace-inbox-page.test.tsx project-job-status-page.test.tsx`
- `npm --workspace saas-pricing-frontend run typecheck`
