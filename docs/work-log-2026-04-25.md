# 2026-04-25 工作日志

## 一、继续推进

今日从 2026-04-24 的 MCP Gateway 端到端联调继续。

本轮完成：

- `apps/mcp-gateway/test/app.test.ts`
  - 新增 `capability URIs are backed by gateway routes`
  - 校验 `RESOURCE_DEFINITIONS` 中每个 URI 都有 `GET` 路由
  - 校验 `TOOL_DEFINITIONS` 中每个 URI 都有 `POST` 路由
  - 防止 capabilities 暴露了不存在的 resource/tool
- `docs/architecture/mcp-capability-design.md`
  - 补 `10.3 当前已落地能力（2026-04-25）`
  - 列出已实现 resources/tools
  - 补充 gateway e2e 覆盖范围和验证命令
- `docs/api/implemented-v1-routes.md`
  - 新增当前 `/v1` 已注册 API 路由清单
  - 标记旧 `openapi-v1.yaml` 仍是历史 `/api/v1` 契约
  - 补路由总数、分组总数摘要
  - 补 HTTP method 分布摘要
  - 补 route 注册源文件摘要
- `tests/api-routes-doc.test.mjs`
  - 新增路由文档覆盖测试
  - 从 `apps/api/src/app` 收集已注册 `/v1` route
  - 对比 `docs/api/implemented-v1-routes.md`
  - 校验 `implemented-v1-routes.md` 由生成脚本生成且无漂移
  - 校验生成文档无 `Other` 兜底分类
  - 校验路由清单摘要计数
  - 校验路由清单 HTTP method 分布
  - 校验路由清单 route 注册源文件
  - 校验 `openapi-v1.yaml` 仍为 18 条旧 `/api/v1` 草案路径，且未混入 `/v1`
  - 校验当前 OpenAPI method 与代码注册路由一致
  - 校验当前 OpenAPI path 参数与代码注册路由一致
- `docs/api/openapi-v1.yaml`
  - 增加历史契约草案提示
  - 指向 `docs/api/implemented-v1-routes.md`
  - 避免继续误用旧 `/api/v1` 路径
- `scripts/generate-implemented-routes-doc.mjs`
  - 新增 API 路由清单生成脚本
  - 从 `apps/api/src/app` 自动收集已注册 `/v1` route
  - 生成 `docs/api/implemented-v1-routes.md`
- `scripts/generate-openapi-v1-current.mjs`
  - 新增当前 `/v1` OpenAPI 最小契约生成脚本
  - 生成 `docs/api/openapi-v1-current.yaml`
  - 路径来自已注册 Fastify routes
  - 补重点 query 参数输出
  - 补 `201 / 202 / 204` 成功响应码推断
  - 补 `integer / boolean / enum` query 参数 schema
  - 补 requestBody required 推断
  - 补通用 `404 / 422` 响应
  - 补下载接口 content-type
  - 补 top-level tags 和 operation tags
  - 补参数 description
  - 补共享 `ErrorResponse / ObjectEnvelope / ListEnvelope` schemas
- `docs/api/openapi-v1-current.yaml`
  - 新增当前代码已注册 `/v1` 的自动生成 OpenAPI skeleton
  - 补 reports / jobs / audit logs / import error report 等 query 参数
  - 补创建、异步任务、删除类接口的成功响应码
  - 补 query 参数类型和常见枚举
  - 补 POST / PUT requestBody required 标记
  - 补 `report export download` 二进制响应
  - 补 `import task error report` 的 `application/json` / `text/csv` 响应
  - 补 API 分组 tags
  - 补参数描述
  - 补共享响应 schema 引用
  - 补 `externalDocs` 指向当前已实现路由清单
  - 补 version compare query 参数描述
- `package.json`
  - `test:workspace` 改为 `node --test tests/*.test.mjs`
  - 新增 `docs:api`
  - 新增 `docs:api-routes`
  - 新增 `docs:openapi-current`
- `docs/api/README.md`
  - 新增 API 契约文档说明
  - 说明当前文件、生成命令、质量门
- `README.md`
  - 补 API 契约文档入口
  - 补 `npm run docs:api`
  - 补 `npm run docs:api-routes`
  - 补 `npm run docs:openapi-current`
  - 补 OpenSpec 规范化索引入口
- `openspec/changes/doc-normalize-v1-to-openspec/specs/error-handling/spec.md`
  - 新增错误处理专题 spec
  - 整理状态码、统一错误结构、锁定态、前端反馈、表格行错误、任务型错误和 AI 失败降级口径
- `openspec/changes/doc-normalize-v1-to-openspec/index.md`
  - 补错误处理专题入口、对应关系和导航分层
- `openspec/changes/doc-normalize-v1-to-openspec/tasks.md`
  - 标记错误处理专题已拆分
- `openspec/changes/doc-normalize-v1-to-openspec/specs/background-job-bus/spec.md`
  - 新增后台任务总线专题 spec
  - 整理任务入队、领取、执行、回写、失败、重试、执行器归属、审计和权限口径
- `openspec/changes/doc-normalize-v1-to-openspec/specs/operations-observability/spec.md`
  - 新增运维观测专题 spec
  - 整理日志分类、健康检查、核心指标、关键任务告警、集中日志、敏感信息脱敏、备份恢复和跨组件追踪口径
- `tests/openspec-doc.test.mjs`
  - 新增 OpenSpec 索引覆盖测试
  - 新增 spec 基础骨架测试
  - 新增错误处理、后台任务总线和运维观测专题任务追踪测试
  - 新增 README OpenSpec 入口测试
  - 新增 OpenSpec non-goals 保持未执行测试
- `apps/api/src/modules/jobs/background-job-service.ts`
  - `completeJob` / `failJob` 增加 processing 状态保护
  - 避免 queued / completed / failed 任务被迟到 worker 回写覆盖终态
- `apps/api/test/background-job-processor.test.ts`
  - 补未启动任务不能 complete 的回归测试
  - 补终态任务不能 fail 的回归测试
- `apps/api/test/app.test.ts`
  - 补 jobs complete/fail 接口层非 processing 任务返回 409 的回归测试
  - 修正 complete/fail 测试标题为 processing jobs
- `apps/worker/src/runtime/api-worker-platform-client.ts`
  - 新增 `WorkerPlatformRequestError`
  - 保留平台响应的 HTTP status 和 error code
- `apps/worker/src/runtime/worker-polling-runner.ts`
  - 忽略迟到上报导致的 `BACKGROUND_JOB_NOT_PROCESSING` 409
  - 其他上报错误继续抛出
- `apps/worker/test/api-worker-platform-client.test.ts`
  - 补平台错误 status/code 保留测试
- `apps/worker/test/worker-polling-runner.test.ts`
  - 补迟到终态冲突不打断 worker 测试
  - 补非迟到平台错误继续抛出测试
- `apps/api/src/app/setup-app-base.ts`
  - `/health` 增加 service / status / checkedAt
  - 保持公开且不暴露依赖、密钥或内部拓扑
- `apps/api/test/app.test.ts`
  - 补 `/health` 最小观测字段断言

## 二、验证

已通过：

- `npm --workspace @saas-pricing/mcp-gateway test`
- `npm --workspace @saas-pricing/mcp-gateway run typecheck`
- `npm run typecheck`
- `npm run test:workspace`
- `npm run docs:api-routes`
- `npm run docs:openapi-current`
- `npm run docs:api`

结果：

- 43 tests
- 43 passed
- typecheck passed
- root typecheck passed
- workspace tests passed, 22 tests
- workspace tests passed, 30 tests
- API route doc regenerated
- current OpenAPI skeleton regenerated

本轮重跑 `npm test`：

- 首次重跑时 `apps/frontend` 出现偶发失败
- 定位到 `project-detail-page.test.tsx` 复制提示未等待异步渲染
- 已改为 `findByText`
- 目标测试通过
- frontend 全量测试通过
- 最终 `npm test` 通过
- 最终 `npm run typecheck` 通过

补充修复：

- `apps/frontend/test/project-detail-page.test.tsx`
  - `已复制当前协作链接，可直接发给协作同事。` 改用异步断言
  - 降低复制反馈渲染时序导致的偶发失败

## 三、下一步

继续优先做可验证收口项：

- 可单独处理前端测试稳定性
- 必要时继续迁移 `openapi-v1.yaml`

## 四、补充：OpenAPI 当前契约收口

已继续完成：

- `docs/api/openapi-v1.yaml`
  - 从历史 `/api/v1` 草案切换为当前代码已注册 `/v1` 的自动生成 OpenAPI 契约
  - 由 `npm run docs:openapi-current` 生成
- `docs/api/openapi-v1-current.yaml`
  - 保留为兼容副本
  - 与 `openapi-v1.yaml` 同步生成并保持一致
- `docs/api/openapi-v1-legacy.yaml`
  - 新增历史 `/api/v1` 草案归档
  - 不再作为当前接口实现依据
- `docs/api/implemented-v1-routes.md`
  - 更新说明：当前契约为 `openapi-v1.yaml`
  - 历史草案归档到 `openapi-v1-legacy.yaml`
- `docs/api/README.md`
  - 更新当前契约文件说明和质量门
- `scripts/generate-openapi-v1-current.mjs`
  - 输出当前契约到 `openapi-v1.yaml`
  - 同步输出兼容副本 `openapi-v1-current.yaml`
- `tests/api-routes-doc.test.mjs`
  - 当前 OpenAPI 校验改为读取 `openapi-v1.yaml`
  - 新增兼容副本一致性校验
  - 历史契约校验改为读取 `openapi-v1-legacy.yaml`
- `docs/architecture/sprint-1-launch-checklist.md`
  - 接口基线说明改为当前 `/v1` 生成契约
- `docs/architecture/team-project-kickoff-brief.md`
  - 接口口径说明改为当前 `/v1` 生成契约

补充验证：

- `npm run docs:api`
- `npm run test:workspace`
- `npm run typecheck`
